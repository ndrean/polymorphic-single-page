const button = document.getElementById('submit')



if (getInput) {
    button.addEventListener('submit', (e) => {
        e.preventDefault();
        const jsrender = document.getElementById('ajax form');
        jsrender.innerHTML=""

    })

}
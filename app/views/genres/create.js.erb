
if (document.getElementById('new-genre')) {
    // const genreId = parseInt(document.activeElement.id.match(/(\d+)/g)[0]);
    target = document.querySelector('.radio-choices');
    
    // essai de mettre l'input à la fin de tous les genres
    targets = document.querySelectorAll('.radio-choices input[type="hidden"');
    Array.from(targets).forEach((currentValue) => {
        console.log(currentValue.name)
    })
    // fin essai
    
    target.insertAdjacentHTML('beforeend', '<%= j render "genres/formgenre", genre: @genre %> ');
    document.getElementById('new_genre').reset()
    };


    
    // document.addEventListener('click', () => {
    //     console.log(document.activeElement); //.nextElementSibling);
    // })

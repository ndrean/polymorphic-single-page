<br>
<%= link_to 'Return', root_path %>
<br>
<ol>
    <% @books.each do |book| %>
        <li>
            <strong><%= book.title %></strong> [ <em><%= book.genre.name %></em> ] 
            by <%= book.author.name %> <%= book.author.country.country %>
            <% (0..5).to_a.sample.times do  %>
                <i class="far fa-star" ></i>
            <% end %>
        </li>  
    <% end %>
</ol>
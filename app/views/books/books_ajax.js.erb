function insertBooks() {
  const divBooks = document.getElementById('ajax-books-all');
  divBooks.innerHTML = "";
  divBooks.insertAdjacentHTML('beforeend',
   '<%= j render "books/books_all", books: @books, cached: true %>')
};

insertBooks();
<ul>
    <% author_books.each do |a|%>
    <li>
      <%= a.name %> <%= a.country.name %>
      <ul>
        <% a.books.each do |b| %>
          <li> <%= b.title %> </li>
        <% end %>
      </ul> 
    <% end %>
    </li>
</ul>